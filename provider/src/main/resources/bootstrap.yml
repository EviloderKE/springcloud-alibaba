spring:
  application:
    name: demo
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:8848
  config:
    activate:
      on-profile: sit
management:
  endpoints:
    web:
      exposure:
        include: "*"